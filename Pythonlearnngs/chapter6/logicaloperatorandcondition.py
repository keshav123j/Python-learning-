#THis is use of and ,or , nor - that is 
#Since we use if(condition true or false)
#Now we can use if(c1 and c2) - if we want the execution only when both true
#if (c1 or c2)- if we want when either 1 is true
#Well datatype wise we enter boolean in if() that is
if(True):
    print("Hello")
else:
    print("None")
    #So this will print Hello