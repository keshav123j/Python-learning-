keshav = "10"
a = keshav
# so a = 10 , now we can convert its datatype to float 
#Now we can convert integer to float , string to float also but it must be an number, not boolean and not none
b = float(a)
print(b)

#Here we are converting it to int , string to int but it must be int 
c = int(a)
print(c)


# seeing if  string of none is actually the word none
d = None
print(str(d))
#Oh yes its indeed the string "None" , Now reverse
e = "None"
print(None(e))
#Ok so no other can be converted to none and boolean 
# but yeah this thing can be used to use + in print and show those together 


